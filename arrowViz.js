


export function ArrowViz(nodelink, viewport) {

    ArrowViz.DEFAULT_RADIUS = 40 * window.devicePixelRatio;
    ArrowViz.DEFAULT_RANGE = Math.PI / 2;
    ArrowViz.HANDLE_SCALE = 1.2;

    this.nodelink = nodelink;
    this.viewport = viewport;


}